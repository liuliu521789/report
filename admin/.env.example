# 本地开发建议留空或不建 .env：管理端 Vite 默认 3000、API 默认 3001，必须不同端口；须另开终端跑后端：cd server && npm run dev
# 改管理端端口时：VITE_ADMIN_DEV_PORT=3000（须与 VITE_APP_API_BASE_URL 中的 API 端口不同）
# 若此处写死 http://localhost:3001，浏览器会直连该端口，后端未启动会出现 net::ERR_CONNECTION_REFUSED。
# API 根：仅协议+主机+端口，不要带 /api（否则请求会变成 /api/api/... 导致 404）
# VITE_APP_API_BASE_URL=
#
# 打包后前端与 API 不同域时再填完整根地址，例如：https://api.example.com
