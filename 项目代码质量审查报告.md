项目代码质量审查报告

一、整体概览

| 指标                | 数值                                                      |
| ------------------- | --------------------------------------------------------- |
| 后端路由文件总行数  | ~9,756 行                                                 |
| 前端 Vue 组件总行数 | ~18,957 行                                                |
| 最大单文件          | sales.js 4,389 行 / SalesOrders.vue 3,234 行              |
| 后端路由处理器总数  | ~72 个（仅 sales.js）                                     |
| 前端最大组件方法数  | ~79 个（SalesOrders.vue）                                 |
| 项目测试文件        | 仅 2 个（securityPolicy.test.js、formatDateTime.test.js） |



------



二、严重问题（"屎山"级别）

1. 巨型文件 - 上帝对象反模式

后端最严重：[sales.js](vscode-file://vscode-app/d:/Qoder/resources/app/out/vs/code/electron-browser/workbench/server/src/routes/sales.js:1) 4,389 行、165KB、72 个路由处理器

这个单文件承载了：订单 CRUD、客户管理、合同管理、字段配置、消息通知、审批流程、Excel 导入导出、二维码绑定...等全部销售模块逻辑。

前端最严重：[SalesOrders.vue](vscode-file://vscode-app/d:/Qoder/resources/app/out/vs/code/electron-browser/workbench/admin/src/views/SalesOrders.vue:1) 3,234 行、106KB、79 个方法、55+ 个响应式状态变量

这是一个典型的 "God Component"，同时管理：订单列表/CRUD、财务审核、发货管理、合同生成/绑定、二维码管理、字段自定义、站内消息收件箱...

其他超大文件：

| 文件                     | 行数  | 问题                                 |
| ------------------------ | ----- | ------------------------------------ |
| public.js                | 1,853 | 1,150 行的 HTML SPA 内嵌在路由代码中 |
| ReportDesigner.vue       | 2,106 | 报告设计器全部逻辑塞在一个组件       |
| ReportEdit.vue           | 1,809 | 同上                                 |
| ContractTemplateEdit.vue | 1,487 | 合同模板编辑                         |
| Layout.vue               | 1,474 | 布局+侧边栏+全部导航逻辑             |
| SalesContracts.vue       | 1,393 | 合同列表管理                         |

2. 业务逻辑与路由处理器耦合（无 Service 层）

所有业务逻辑（状态机、通知、审批流）直接写在路由 handler 的 async (req, res, next) => { ... } 中，没有独立的 service 层抽象。这意味着：

无法单元测试业务逻辑

无法复用跨路由的业务规则

修改任何业务规则都要在巨型文件中定位

3. 大量重复代码模式

权限检查（50+ 处重复）：

javascript

if (!perm(req, 'order_management', 'order_query'))  return res.status(403).json({ error: 'FORBIDDEN' });

分页参数解析（8+ 处重复）：

javascript

const page = Math.max(1, parseInt(req.query.page) || 1);const pageSize = Math.min(100, Math.max(10, parseInt(req.query.pageSize) || 20));

资源查找 + 404 检查（15+ 处重复）：

javascript

const [rows] = await pool.query('SELECT * FROM table WHERE id = ?', [id]);if (!rows[0]) return res.status(404).json({ error: 'NOT_FOUND' });

批量操作模板（4+ 处复制粘贴）：获取连接、开事务、FOR UPDATE、循环处理、提交、释放 — 每个批量接口重复 ~100 行。



------



三、架构问题

1. 缺少分层架构

当前结构：

plaintext

Route Handler → 直接操作 DB Pool → 直接发通知

建议结构：

plaintext

Route Handler → Service → Repository/DAO            → NotificationService            → AuditService

2. public.js 中内嵌完整 HTML SPA

[public.js](vscode-file://vscode-app/d:/Qoder/resources/app/out/vs/code/electron-browser/workbench/server/src/routes/public.js:59) 的 sendScanLandingHtml() 函数长达 1,154 行，是一个完整的客户端 SPA（含 HTML、CSS、JavaScript）以字符串模板的形式嵌入后端代码。这违反了前后端分离原则，极难维护。

3. 前端无 composable 抽象

SalesOrders.vue 的 55+ 个状态变量和 79 个方法全部平铺在单个组件中，没有使用 Vue 3 Composable 进行逻辑拆分。至少可以拆出：

useOrderQcBind() - 二维码绑定逻辑 (~250 行)

useContractManagement() - 合同管理逻辑 (~350 行)

useOrderMessages() - 消息管理逻辑 (~150 行)

useFieldManagement() - 字段配置逻辑 (~120 行)

useOrderForm() - 表单管理逻辑 (~100 行)

4. 魔法字符串散布

订单状态（'pending_review', 'approved', 'shipped'...）、权限模块名（'order_management'）、员工类别（'finance', 'warehouse'）全部是硬编码字符串，没有定义为常量。



------



四、安全问题

| 问题                     | 严重度 | 说明                                                         |
| ------------------------ | ------ | ------------------------------------------------------------ |
| 无 CSRF 保护             | 中     | 状态变更操作（POST/PUT/DELETE）依赖 same-origin，没有 CSRF token |
| CORS 全开                | 中     | app.use(cors()) 无参数 = 允许所有来源                        |
| 前端 localStorage 存权限 | 低     | 权限 JSON 存在 localStorage，XSS 可篡改（但服务端会重新校验） |
| 公开页面暴露错误信息     | 低     | public.js 扫码页面将原始错误 message 展示给终端用户          |
| SQL 注入                 | 安全   | 全部使用参数化查询 ? 占位符                                  |
| 路径穿越                 | 安全   | 文件操作有 .. 检查和 startsWith(root) 校验                   |



------



五、测试覆盖

几乎为零。整个项目仅有 2 个测试文件：

server/src/lib/securityPolicy.test.js

admin/src/utils/formatDateTime.test.js

4,389 行的 sales.js、3,234 行的 SalesOrders.vue 等核心业务代码完全没有测试覆盖。



------



六、错误处理不一致

public.js 返回纯文本错误：res.status(400).type('text').send('bad token')

sales.js 返回 JSON 错误：res.status(400).json({ error: 'BAD_REQUEST' })

auth.js 用 Error 对象附加 code：e.code = 'INVALID_PENDING_TOKEN'

全局中间件统一兜底但无法修正上游不一致



------



七、重构优先级建议

P0 - 紧急（降低维护风险）

拆分 sales.js：按业务域拆为独立路由文件

routes/sales/orders.js - 订单 CRUD + 状态流转

routes/sales/customers.js - 客户管理

routes/sales/contracts.js - 合同管理

routes/sales/internal-models.js - 内部型号

routes/sales/messages.js - 站内消息

拆分 SalesOrders.vue：拆成子组件 + composable

至少拆出 5 个 composable 和 4-5 个子组件

将 public.js 中的 HTML SPA 提取为独立文件

P1 - 重要（提升代码质量）

引入 Service 层：将业务逻辑从路由处理器中提取出来

抽取通用中间件：权限检查、分页参数、资源查找+404

定义状态/权限常量：消除魔法字符串

统一错误响应格式

P2 - 改进（长期健康）

补充单元测试：优先覆盖 Service 层核心业务逻辑

添加 CSRF 保护

收紧 CORS 配置

前端 API 层重构：api/index.js 的 150+ 个扁平函数改为资源化组织



------



总结：项目的核心功能是完整的，SQL 注入防护等基本安全做得不错。但 sales.js（4,389 行）和 SalesOrders.vue（3,234 行）已经是典型的"屎山"级别文件，主要问题是缺少分层抽象、代码大量重复、巨型文件难以维护。如果需要，我可以开始对具体某个模块进行重构。

 